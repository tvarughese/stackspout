apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: add-do
  namespace: flux-system
spec:
  interval: 10m
  prune: true
  path: ./apps/do
  sourceRef:
    kind: GitRepository
    name: stackspout
