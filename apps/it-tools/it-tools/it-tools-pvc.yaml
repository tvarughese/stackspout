apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: it-tools-data
  namespace: stackspout
  labels:
    stackspin.net/backupSet: "it-tools"
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
